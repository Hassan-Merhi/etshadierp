{
  "version": 1,
  "description": "Diagnostic-only total threshold on the temporary static-build branch. Main remains unchanged.",
  "scan": {
    "patterns": [
      "client/src/**/*.{ts,tsx}",
      "server/**/*.ts",
      "shared/**/*.ts"
    ],
    "reportPath": "artifacts/lint/eslint-report.json",
    "step": 500
  },
  "totals": {
    "warningCeiling": 100000,
    "errorCeiling": 0
  },
  "perRule": {
    "react-hooks/exhaustive-deps": 100000,
    "no-control-regex": 100000
  },
  "notes": {
    "diagnostic": "Temporary branch only; never merge this threshold."
  }
}
