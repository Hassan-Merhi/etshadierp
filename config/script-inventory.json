{
  "version": 1,
  "description": "Which scripts/verify-*.mjs and scripts/audit-*.mjs are gates and which are decoration. A wired script must pass; the orphan count may only fall. Scripts that legitimately need a build artifact, a database, or the network are listed in environmentDependent with the reason, and are not executed by the audit. knownFailing records gates that were already failing when this audit was written, each with the reason; the list may only shrink, and a script that starts passing must come off it. The ceiling moved 17 -> 18 once when deleting two dead release workflows orphaned verify-sp-migration-rehearsal.mjs; it is retained rather than deleted because the SP migration routes it rehearses are still live, and it needs a database so it cannot simply be added to the main CI run.",
  "orphanCeiling": 18,
  "environmentDependent": {
    "verify-server-bundle.mjs": "Reads dist/ \u2014 requires npm run build.",
    "verify-runtime-dependencies.mjs": "Reads esbuild's metafile from dist/ \u2014 requires npm run build.",
    "verify-production-artifact.mjs": "Inspects the built production artifact.",
    "verify-dependency-audit.mjs": "Runs npm audit, which needs registry access.",
    "verify-ci-disposable-schema.mjs": "Provisions and drops a throwaway database.",
    "verify-sp-migration-rehearsal.mjs": "Executes a migration rehearsal against a database.",
    "verify-database-backup.mjs": "Exercises backup and restore against a database.",
    "verify-memory-stabilization.mjs": "Measures runtime memory against a running server.",
    "audit-coverage-ratchet.mjs": "Reads coverage/ \u2014 requires a coverage run.",
    "audit-god-file-boundaries.mjs": "Reports reviewed growth allowances as failures; the gate is tests/god-file-boundaries.test.ts, which consults config/ci-ratchet-allowances.json."
  },
  "knownFailing": {
    "audit-i18n-phase14.mjs": "Actionable-literal ratchet is already breached on origin/main (12,348 -> 12,355 there). Wired into four workflows and failing in all of them before this audit existed.",
    "verify-phase9-final-i18n-baseline.mjs": "Same breached i18n literal baseline as audit-i18n-phase14.",
    "verify-multilingual-phases-4-7-current-main.mjs": "Reviewed-count contracts (248 reports, 405 backend messages) no longer match the source.",
    "verify-mobile-responsive-phase11-regression.mjs": "Asserts a literal queryClient.invalidateQueries({ refetchType: \"active\" }) call that has since been reworded.",
    "audit-program6d-database-query-risks.mjs": "package.json invokes it with --json; run bare it reports findings and exits non-zero by design."
  }
}
