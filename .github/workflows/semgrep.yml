name: Semgrep

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "41 5 * * 3"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: semgrep-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  regression-scan:
    name: Semgrep CE new-findings gate
    if: >-
      github.actor != 'dependabot[bot]' &&
      (github.event_name == 'pull_request' || github.event_name == 'push')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    container:
      image: semgrep/semgrep:1.172.0@sha256:65dcd4408adda7c183a6b4550cb1e9b19f7f627a6fbb7e0559bd466bedc44d7b
    env:
      SEMGREP_BASELINE_COMMIT: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # The regression scan must be able to resolve the exact PR base SHA.
          # A shallow checkout can omit it on long-lived/diverged branches,
          # causing the gate to fail before Semgrep scans any code.
          fetch-depth: 0

      - name: Validate baseline commit
        shell: sh
        run: |
          case "$SEMGREP_BASELINE_COMMIT" in
            ""|0000000000000000000000000000000000000000)
              echo "A usable baseline commit is required for the Semgrep regression gate."
              exit 2
              ;;
          esac
          if ! printf '%s' "$SEMGREP_BASELINE_COMMIT" | grep -Eq '^[0-9a-f]{40}$'; then
            echo "Semgrep baseline is not a 40-character commit SHA."
            exit 2
          fi
          git config --global --add safe.directory "$PWD"
          git cat-file -e "${SEMGREP_BASELINE_COMMIT}^{commit}"
          git merge-base "$SEMGREP_BASELINE_COMMIT" HEAD >/dev/null

      - name: Scan for newly introduced findings
        run: semgrep scan --config auto --baseline-commit "$SEMGREP_BASELINE_COMMIT" --error

  full-audit:
    name: Semgrep CE full repository audit
    if: >-
      github.actor != 'dependabot[bot]' &&
      (github.event_name == 'schedule' || github.event_name == 'workflow_dispatch')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    container:
      image: semgrep/semgrep:1.172.0@sha256:65dcd4408adda7c183a6b4550cb1e9b19f7f627a6fbb7e0559bd466bedc44d7b

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Audit complete repository
        run: semgrep scan --config auto
