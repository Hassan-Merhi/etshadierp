name: Phase 18 Toolchain Repair V2
on:
  push:
    branches: [main]
permissions:
  contents: write
env:
  CI: true
jobs:
  repair:
    if: contains(github.event.head_commit.message, '[phase18-toolchain-repair-v2]')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: '22.14'
          cache: npm
      - name: Normalize exact stale runtime literals
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          old='22.14'+'.0'
          new=Path('.node-version').read_text().strip()
          changed=[]
          for p in sorted(Path('.github/workflows').glob('*.yml')) + sorted(Path('.github/workflows').glob('*.yaml')):
              s=p.read_text()
              out=s.replace(old,new)
              if out != s:
                  p.write_text(out)
                  changed.append(str(p))
          print('NORMALIZED='+str(len(changed)))
          for p in changed: print(p)
          PY
      - name: Verify canonical toolchain coherence
        run: node scripts/audit-toolchain-coherence.mjs
      - name: Commit and push verified normalization
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add .github/workflows
          if git diff --cached --quiet; then
            echo 'NO_TOOLCHAIN_CHANGES=1'
            exit 0
          fi
          git commit -m 'ci: normalize all workflow Node pins [phase18-toolchain-fixed] [skip ci]'
          for attempt in 1 2 3 4 5; do
            git fetch origin main
            if git rebase origin/main && node scripts/audit-toolchain-coherence.mjs && git push origin HEAD:main; then
              exit 0
            fi
            git rebase --abort || true
            sleep 2
          done
          exit 1
