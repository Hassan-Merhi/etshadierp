name: Phase 18 Wave 2 Phase 1 Safe Inference
on:
  pull_request:
    types: [opened, synchronize, reopened]
permissions:
  contents: write
jobs:
  execute:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by phase18-phase1-safe-sharded.yml'
