name: Apply bandwidth phases 5-6

on:
  push:
    branches:
      - agent/bandwidth-phases-5-6

permissions:
  contents: write

concurrency:
  group: apply-bandwidth-phases-5-6
  cancel-in-progress: true

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/bandwidth-phases-5-6
          fetch-depth: 0

      - name: Apply Phase 5-6 implementation
        run: |
          python scripts/apply-bandwidth-phases-5-6.py
          python scripts/finalize-bandwidth-phases-5-6.py

      - name: Remove temporary implementation machinery
        run: |
          rm -f scripts/apply-bandwidth-phases-5-6.py
          rm -f scripts/finalize-bandwidth-phases-5-6.py
          rm -f .github/workflows/apply-bandwidth-phases-5-6.yml

      - name: Commit completed source
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Complete bandwidth container, Daybook and voucher batching phases"
          git push origin HEAD:agent/bandwidth-phases-5-6
