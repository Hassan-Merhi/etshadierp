name: Apply production comparison UI patch

on:
  push:
    branches:
      - "fix/production-comparison-arabic-workers-hover"

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test
        run: echo "workflow started"
