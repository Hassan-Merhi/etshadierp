name: Combo 4G Apply

on:
  push:
    branches:
      - agent/combo-4g-final-data-model-typescript
    paths:
      - .github/workflows/combo4g-apply.yml
      - scripts/combo-4g-apply.mjs

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Combo 4G branch
        uses: actions/checkout@v4
        with:
          ref: agent/combo-4g-final-data-model-typescript
          persist-credentials: true

      - name: Apply audited data-model fixes
        run: node scripts/combo-4g-apply.mjs

      - name: Commit production changes and cleanup
        run: |
          rm -f scripts/combo-4g-apply.mjs .github/workflows/combo4g-apply.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Resolve final TypeScript data-model diagnostics"
          git push origin HEAD:agent/combo-4g-final-data-model-typescript
