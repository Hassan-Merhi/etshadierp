name: Phase 15 Final 100/100 Certification

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/phase15-final-certification.yml"
  workflow_dispatch:

permissions:
  contents: read
  statuses: write

concurrency:
  group: phase15-final-certification-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  certify:
    name: Final 100/100 certification
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: heliumdb
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d heliumdb"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 12
    env:
      NODE_ENV: test
      SESSION_SECRET: phase15-final-certification-secret
      CSRF_ENFORCE: "0"
      ENABLE_SCHEDULERS: "false"
      DR_REHEARSAL_RTO_SECONDS: "300"
      PGHOST: localhost
      PGPORT: "5432"
      PGUSER: postgres
      PGPASSWORD: postgres
      PGSSLMODE: disable
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/heliumdb
      PORT: "5000"
      CERT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
    steps:
      - name: Mark Phase 15 certification pending
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          curl --fail-with-body --request POST \
            --header "Accept: application/vnd.github+json" \
            --header "Authorization: Bearer ${GH_TOKEN}" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/statuses/${CERT_SHA}" \
            --data '{"state":"pending","context":"phase15/final-100-certification","description":"Phase 15 final certification is running"}'

      - name: Checkout exact PR head
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ env.CERT_SHA }}
          persist-credentials: false
          fetch-depth: 2

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: "24.19.0"
          cache: npm

      - name: Normalize package registry URLs
        run: sed -i 's#http://package-firewall.replit.local/npm/#https://registry.npmjs.org/#g' package-lock.json

      - name: Install dependencies
        id: install
        continue-on-error: true
        env:
          SKIP_GIT_HOOKS: "1"
        run: npm ci --no-audit --bin-links=true

      - name: Roadmap invariant and static certification
        id: static
        if: always() && steps.install.outcome == 'success'
        continue-on-error: true
        run: |
          set -euo pipefail
          npm run verify:lockfile
          npm run verify:production-dependencies
          npm run audit:doc-index
          npm run audit:toolchain
          npm run audit:scripts
          npm run audit:god-files
          npm run audit:type-escapes
          npm run audit:write-routes
          npm run audit:write-evidence
          npm run audit:company-scope
          npm run audit:inventory-contracts
          npm run audit:query-risks
          npm run audit:platform
          npm run test:platform
          npm run format:check:changed -- --base HEAD^
          npm run lint
          npm run audit:lint-ratchet
          npm run check
          npm run build
          npm run verify:server-bundle
          npm run verify:final-production-readiness
          npm run verify:observability
          node scripts/verify-phase11-12-observability-disaster-recovery.mjs
          npm run verify:stabilization
          npm run verify:mobile-web-routing
          npm run verify:bandwidth
          node scripts/verify-program7d-accessibility-responsive.mjs

      - name: Prepare and verify disposable PostgreSQL schema
        id: schema
        if: always() && steps.install.outcome == 'success' && steps.static.outcome == 'success'
        continue-on-error: true
        run: |
          set -euo pipefail
          node scripts/verify-migration-registry.mjs --json
          node node_modules/drizzle-kit/bin.cjs push --force
          node scripts/verify-ci-disposable-schema.mjs

          node dist/index.js > "$RUNNER_TEMP/phase15-startup-migrations.txt" 2>&1 &
          app_pid=$!
          cleanup() {
            kill "$app_pid" 2>/dev/null || true
            wait "$app_pid" 2>/dev/null || true
          }
          trap cleanup EXIT

          for _attempt in $(seq 1 180); do
            if curl --fail --silent --show-error "http://127.0.0.1:${PORT}/api/health/db"; then
              echo
              exit 0
            fi
            if ! kill -0 "$app_pid" 2>/dev/null; then
              echo "Application exited before startup migrations became ready."
              cat "$RUNNER_TEMP/phase15-startup-migrations.txt"
              exit 1
            fi
            sleep 1
          done

          echo "Timed out waiting for application startup migrations."
          cat "$RUNNER_TEMP/phase15-startup-migrations.txt"
          exit 1

      - name: Backend regression and coverage certification
        id: backend
        if: always() && steps.schema.outcome == 'success'
        continue-on-error: true
        run: |
          set -euo pipefail
          npm run test:backend:verify
          npm run test:backend:verify:coverage
          npm run test:smoke-sweep

      - name: Frontend regression and coverage certification
        id: frontend
        if: always() && steps.install.outcome == 'success' && steps.static.outcome == 'success'
        continue-on-error: true
        run: |
          set -euo pipefail
          npm run test:frontend
          npm run test:frontend:coverage

      - name: Coverage ratchet certification
        id: coverage
        if: always() && steps.backend.outcome == 'success' && steps.frontend.outcome == 'success'
        continue-on-error: true
        run: npm run audit:coverage-ratchet

      - name: Final security certification
        id: security
        if: always() && steps.schema.outcome == 'success'
        continue-on-error: true
        run: |
          set -euo pipefail
          npm run check:security
          npm run verify:dependency-audit
          npm audit --omit=dev --audit-level=critical

      - name: Disaster recovery certification
        id: resilience
        if: always() && steps.schema.outcome == 'success'
        continue-on-error: true
        run: |
          set -euo pipefail
          psql "$DATABASE_URL" -v ON_ERROR_STOP=1 <<'SQL'
          CREATE TABLE phase15_rehearsal_sentinel (id integer PRIMARY KEY, marker text NOT NULL);
          INSERT INTO phase15_rehearsal_sentinel (id, marker) VALUES (1, 'phase15-final-certification');
          SQL

          psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -At -F $'\t' <<'SQL' > "$RUNNER_TEMP/phase15-source-critical-counts.tsv"
          SELECT 'companies', COUNT(*)::bigint FROM companies
          UNION ALL SELECT 'inventory', COUNT(*)::bigint FROM inventory
          UNION ALL SELECT 'ledger_accounts', COUNT(*)::bigint FROM ledger_accounts
          UNION ALL SELECT 'voucher_entries', COUNT(*)::bigint FROM voucher_entries
          UNION ALL SELECT 'vouchers', COUNT(*)::bigint FROM vouchers
          ORDER BY 1;
          SQL

          BACKUP_FILE="$RUNNER_TEMP/phase15.dump"
          pg_dump --format=custom --no-owner --no-acl --file="$BACKUP_FILE" "$DATABASE_URL"
          pg_restore --list "$BACKUP_FILE" > "$RUNNER_TEMP/phase15.toc.txt"
          test -s "$RUNNER_TEMP/phase15.toc.txt"
          node scripts/verify-database-backup.mjs "$BACKUP_FILE" --max-age-hours=1 --json

          dropdb --if-exists -h localhost -U postgres heliumdb_phase15_restore
          createdb -h localhost -U postgres heliumdb_phase15_restore
          RESTORE_URL=postgresql://postgres:postgres@localhost:5432/heliumdb_phase15_restore
          started_at=$(date +%s)
          pg_restore --exit-on-error --no-owner --no-acl --dbname="$RESTORE_URL" "$BACKUP_FILE"
          restore_seconds=$(( $(date +%s) - started_at ))
          if [ "$restore_seconds" -gt "$DR_REHEARSAL_RTO_SECONDS" ]; then
            echo "Restore took ${restore_seconds}s, exceeding the ${DR_REHEARSAL_RTO_SECONDS}s rehearsal recovery-time budget."
            exit 1
          fi

          test "$(psql "$RESTORE_URL" -Atqc "SELECT marker FROM phase15_rehearsal_sentinel WHERE id=1")" = "phase15-final-certification"
          DATABASE_URL="$RESTORE_URL" node scripts/verify-ci-disposable-schema.mjs
          psql "$RESTORE_URL" -v ON_ERROR_STOP=1 -At -F $'\t' <<'SQL' > "$RUNNER_TEMP/phase15-restore-critical-counts.tsv"
          SELECT 'companies', COUNT(*)::bigint FROM companies
          UNION ALL SELECT 'inventory', COUNT(*)::bigint FROM inventory
          UNION ALL SELECT 'ledger_accounts', COUNT(*)::bigint FROM ledger_accounts
          UNION ALL SELECT 'voucher_entries', COUNT(*)::bigint FROM voucher_entries
          UNION ALL SELECT 'vouchers', COUNT(*)::bigint FROM vouchers
          ORDER BY 1;
          SQL
          diff -u "$RUNNER_TEMP/phase15-source-critical-counts.tsv" "$RUNNER_TEMP/phase15-restore-critical-counts.tsv"
          node node_modules/vitest/vitest.mjs run tests/scheduler-tick-guard.test.ts --maxWorkers=1 --no-file-parallelism

          {
            echo "## Phase 15 disaster-recovery certification"
            echo
            echo "- Restore duration: ${restore_seconds}s"
            echo "- Rehearsal recovery-time budget: ${DR_REHEARSAL_RTO_SECONDS}s"
            echo "- Critical-row fingerprint parity: passed"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Publish authoritative Phase 15 status
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          INSTALL: ${{ steps.install.outcome }}
          STATIC: ${{ steps.static.outcome }}
          SCHEMA: ${{ steps.schema.outcome }}
          BACKEND: ${{ steps.backend.outcome }}
          FRONTEND: ${{ steps.frontend.outcome }}
          COVERAGE: ${{ steps.coverage.outcome }}
          SECURITY: ${{ steps.security.outcome }}
          RESILIENCE: ${{ steps.resilience.outcome }}
        run: |
          set -euo pipefail
          state=success
          for result in "$INSTALL" "$STATIC" "$SCHEMA" "$BACKEND" "$FRONTEND" "$COVERAGE" "$SECURITY" "$RESILIENCE"; do
            if [ "$result" != "success" ]; then state=failure; fi
          done
          description="Phase 15 final 100/100 certification passed"
          if [ "$state" != "success" ]; then description="Phase 15 final 100/100 certification failed"; fi
          payload=$(printf '{"state":"%s","context":"phase15/final-100-certification","description":"%s"}' "$state" "$description")
          curl --fail-with-body --request POST \
            --header "Accept: application/vnd.github+json" \
            --header "Authorization: Bearer ${GH_TOKEN}" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/statuses/${CERT_SHA}" \
            --data "$payload"
          printf 'install=%s\nstatic=%s\nschema=%s\nbackend=%s\nfrontend=%s\ncoverage=%s\nsecurity=%s\nresilience=%s\n' \
            "$INSTALL" "$STATIC" "$SCHEMA" "$BACKEND" "$FRONTEND" "$COVERAGE" "$SECURITY" "$RESILIENCE"
          test "$state" = "success"
