name: Wave H Phase 2 Format

on:
  push:
    branches: [wave-i/convergence-automation]
    paths:
      - tests/ui/wave-h-zero-coverage-surfaces.test.tsx
      - .github/workflows/wave-h-phase2-format.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: wave-i/convergence-automation
      - uses: actions/setup-node@v6
        with:
          node-version: "22.14.0"
          cache: npm
      - run: sed -i 's#http://package-firewall.replit.local/npm/#https://registry.npmjs.org/#g' package-lock.json
      - run: npm ci --ignore-scripts --no-audit
      - run: node node_modules/prettier/bin/prettier.cjs --write tests/ui/wave-h-zero-coverage-surfaces.test.tsx
      - name: Commit formatting when needed
        run: |
          if git diff --quiet -- tests/ui/wave-h-zero-coverage-surfaces.test.tsx; then
            echo "Already formatted."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- tests/ui/wave-h-zero-coverage-surfaces.test.tsx
          git commit -m "style: format Wave H Phase 2 tests"
          git push origin HEAD:wave-i/convergence-automation
