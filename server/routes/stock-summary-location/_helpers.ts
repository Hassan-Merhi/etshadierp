/**
 * Shared state and helpers for the stockSummaryLocationRoutes routes.
 *
 * Extracted verbatim from the former single-file stockSummaryLocationRoutes.ts.
 */

/**
 * Location-scoped stock-summary routes.
 *
 * Per-location + per-stock-item monthly summary, monthly detail, the location
 * matrix view, and the location transactions ledger. Extracted from
 * stockSummaryRoutes.ts as a sub-registrar; behaviour is unchanged.
 */
