You are working inside my existing GitHub project. Please analyze the current codebase and implement/fix global multi-currency behavior across BOTH display + data entry + voucher/payment flows, AND fix the missing “daily rate selection” popup.

A) Global currency toggle (display)
1) In multi-currency countries, there is a currency button in the top header (example: “USD/CFA” or whatever label exists today).
2) When I click it, EVERYTHING on the site/app must immediately change to the other currency (all pages/components where currency is shown).
3) This must work for ALL users (guest + logged-in).

Acceptance Criteria (display)
- All currency displays update instantly (no full refresh): product prices, totals, cart/checkout summaries, fees, shipping, taxes, subscriptions, invoices, any price badges, etc.
- Hide/disable the toggle if the country is NOT multi-currency.
- Use a single formatting utility (Intl.NumberFormat or existing project utility).
- Use a single global currency state (Context/Redux/store/etc.) as the only source of truth.

Persistence
- Guests: persist in localStorage or cookie and restore on reload.
- Logged-in users: also persist to backend user settings/profile if such model exists, so it follows across devices.

Server/API integration
- If API supports a currency param/header, include the selected currency in requests that return prices (products, cart, checkout, voucher endpoints, etc.).
- Avoid mixing currencies in the same totals.

B) Currency toggle must control ALL amount entry fields (vouchers + payments + any “enter amount” screens)
Problem
- When I toggle to CFA, the Vouchers flow still only allows entering/charging in USD.
- This happens on every page where I enter payment-related data.

Goal / Acceptance Criteria (inputs)
- Every amount input must follow the selected currency:
  - Vouchers: create, redeem, apply voucher amount, voucher balance display, etc.
  - Checkout/payment: payment amount inputs, partial payments, tips/fees, “custom amount” fields, etc.
  - Any admin/merchant/manual adjustment screens (if they exist).
- The currency label/symbol next to the input updates instantly on toggle.
- Validation matches selected currency (min/max, decimals, step).
- Submissions/payloads include the correct currency consistently.
- If backend stores amounts in a base currency, convert deterministically; otherwise switch the per-currency field returned by API.

Implementation guidance (inputs)
- Remove any hard-coded “USD” in voucher/payment forms.
- Standardize utilities:
  - parseAmount(inputString, currency, locale)
  - formatAmount(value, currency, locale)
  - convertAmount(value, fromCurrency, toCurrency, rateSource) ONLY if needed
- Ensure switching currency while typing does not break input; preserve value safely and reformat on blur.
- Prevent currency mixing in a single transaction unless explicitly supported.

C) Fix: “Daily rate selection” popup does not show
Problem
- The popup/modal/dropdown that lets the user choose/select a “daily rate” is not appearing (it should show when the user reaches the relevant step/screen/action).

Goal / Acceptance Criteria (daily rate popup)
- Identify where the daily rate selection popup is supposed to trigger (button click, page load, step in a flow, condition based on country/currency, etc.).
- Fix the trigger so the popup reliably appears when expected.
- Ensure the popup displays the correct currency context:
  - If daily rates are currency-dependent, the list/options should update when currency is toggled.
  - If rates are stored in base currency, convert/format for display consistently using the same global currency logic.
- Ensure the selected daily rate is persisted correctly (state + submission payload) and does not revert to USD-only behavior.

Implementation Steps (do these in order)
1) Scan repo for currency + voucher + payment + daily rate code:
   Search terms: “currency”, “USD”, “CFA”, “Intl.NumberFormat”, “formatPrice”, “exchangeRate”, “voucher”, “redeem”, “applyVoucher”, “amount”, “payment”, “checkout”, “daily rate”, “dailyRate”, “ratePopup”, “modal”, “dialog”.
2) Locate the header currency button component and confirm current currency/country detection logic.
3) Implement/extend global CurrencyProvider/store:
   - state: selectedCurrency, availableCurrencies
   - actions: toggleCurrency(), setCurrency()
   - persistence for guests + logged-in users
4) Refactor ALL currency rendering to read from global store + single formatPrice/formatAmount.
5) Refactor ALL amount input components and voucher/payment flows to use selectedCurrency for:
   - displayed currency
   - validation
   - payload currency
   - API params/headers
6) Debug the daily rate popup:
   - Find the popup component and its open/close state and trigger conditions.
   - Fix the trigger wiring (event handlers, conditional rendering, route guards, feature flags, z-index/portal issues, missing provider, etc.).
   - Confirm it renders and is not hidden by CSS (display/opacity/z-index), not blocked by unmounted state, and not failing due to JS errors.
   - Ensure it responds to currency toggles and submits correct currency/rate data.
7) Tests + verification:
   - Add/update tests:
     - currency toggle updates a price component and cart total
     - currency toggle updates voucher amount input + allows submitting in CFA
     - daily rate popup appears when expected and shows correctly formatted currency
   - Provide a manual QA checklist for:
     - guest vs logged-in
     - across key pages (product list, product detail, cart, checkout, vouchers, daily rate screen)

Output Requirements
- Provide a clear summary of changes with file paths and why they were changed.
- List every modified file.
- Explain how amounts are stored (base currency vs per-currency fields) and how conversion is handled.
- Include key snippets (only the most relevant ones) showing:
  - currency store/provider
  - updated amount input handling
  - daily rate popup trigger fix
- Keep UI design unchanged; only fix behavior.
- Follow existing project patterns (React Context vs Redux vs Zustand, existing API layer, existing modal/dialog components).

Start by scanning the repo for all the above keywords, then implement the fixes systematically.
