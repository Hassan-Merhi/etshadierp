You are working in a React + TypeScript app using react-hook-form + Zod. The Stock Transfer form “Save” button does nothing (no API call) because Zod validation fails: sourceLocationId must be > 0 but stays 0 when selecting an item from the inventory sidebar / Enter selection.

GOAL:
Implement a real fix so selecting an item ALWAYS sets a valid sourceLocationId (number > 0) into the react-hook-form field being validated, and validation errors are visible (no silent fail).

CONTEXT:
- Form submit uses: handleSubmit(onStockTransferSubmit, errorHandler)
- Zod schema requires sourceLocationId > 0 and stockItemId > 0
- When an item is selected, code currently sets stockItemId, stockItemName, rate but NOT sourceLocationId.
- I attempted to set it from transferInventorySource but it still fails.

DO THIS FIX (must be robust):
1) In BOTH item-selection handlers (sidebar click + Enter key selection), do NOT rebuild the entire entries array in a way that desyncs RHF. Instead, set the specific fields using stockTransferForm.setValue with the correct path:
   - entries.${index}.stockItemId
   - entries.${index}.stockItemName
   - entries.${index}.rate
   - entries.${index}.sourceLocationId
   - entries.${index}.sourceLocationName

2) Ensure transferInventorySource is coerced to a number:
   const sourceId = Number(transferInventorySource);
   Use sourceId > 0 checks. Do NOT use `if (transferInventorySource)` because 0 is falsy and strings can cause issues.

3) Call setValue with shouldValidate/shouldDirty enabled at least for sourceLocationId and stockItemId:
   setValue(path, value, { shouldValidate: true, shouldDirty: true, shouldTouch: true })

4) If the form uses useFieldArray, use fieldArray.update(index, updatedEntry) OR setValue per-field (preferred). Do NOT only setValue("entries", newEntries) unless you also ensure shouldValidate and correct field name. Confirm the actual field name used in useForm/defaultValues/schema (entries vs transferEntries). Fix mismatches.

5) Make validation failures visible:
   In the error handler passed to handleSubmit, show a toast message that includes the first relevant error message (e.g., “Source location is required”). Keep console.error too.

6) OPTIONAL but recommended:
   Update Zod schema to tolerate string inputs from selects:
   sourceLocationId: z.coerce.number().gt(0)
   stockItemId: z.coerce.number().gt(0)

DELIVERABLE:
- Provide the exact code changes (diff/patch style) in the correct file(s).
- Identify the exact component file(s) where item selection occurs and apply the fix there.
- After fix, selecting an item should set sourceLocationId immediately, and Save should call onStockTransferSubmit.
- If sourceId is not valid (>0), show a toast warning like “Select a source location first” and prevent adding the item.

IMPORTANT:
Do not hand-wave. Actually implement the code changes in the files. Ensure the RHF field path matches the schema and submit handler.
