I messed up the currency logic. I need a dual-currency system (CDF + USD) without auto-conversion.

GOAL
- The app must support transactions/POS in either CDF or USD.
- User chooses the currency per transaction using a clear toggle/button (CDF / USD).
- This currency selector must be available on ALL pages where money is entered or displayed (POS, transactions, invoices, expenses, payments, etc.).

IMPORTANT: NO AUTO-CONVERSION
- Stop converting USD amounts into CDF (or CDF into USD) based on a “today rate”.
- Exchange rate should not be forced/prompted every time just to display balances.
- Only store amounts in the currency they were entered.

DASHBOARD REQUIREMENTS
- Dashboard must show balances in BOTH currencies, but as separate totals:
  - Total CDF (sum of all CDF entries)
  - Total USD (sum of all USD entries)
- Do NOT “convert accounts” to show both. Right now if I enter USD it converts to CDF using today’s rate — remove that behavior.
- Accounts should not appear as “both CDF/USD” by conversion. They should simply accumulate in their own currency.

DATA MODEL / STORAGE
- Every monetary record must have:
  - amount (numeric)
  - currency (ENUM: 'CDF' or 'USD')
- Any computed totals must group by currency, not convert.

UI/UX
- Add a currency switch control (segmented button or dropdown) defaulting to last-used currency.
- Display amounts with currency label everywhere (e.g., “$ 10.00”, “CDF 25,000”).
- Filters/report pages should allow viewing totals by currency.

REVERSING PREVIOUS SQL WORK
- Previously we added “sql stuff” that shouldn’t be there.
- Please revert the DB/schema/migrations and code changes related to that SQL approach and return to the state before those changes.
- If full revert isn’t possible, create a new migration that removes the added tables/columns and deletes/undoes the conversion logic so the app works with the dual-currency model above.
- Tell me exactly which migrations/commits/files you reverted or replaced.

Deliverables
1) Implement currency selector across all relevant pages.
2) Remove auto-conversion and “today rate” prompt logic.
3) Update queries/calculations to group totals by currency.
4) Revert/remove the unwanted SQL changes (or undo them via new migrations) and confirm the app still runs.
