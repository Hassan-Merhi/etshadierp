You are working inside my existing GitHub project. Please analyze the codebase and fix exchange-rate + currency parsing issues for POS, payments, and receipts.

What I need (2 issues)

1) Historical transactions must stay on the rate they were recorded with
- Anything created “on a rate” (POS sale, payment, receipt, voucher redemption, invoice payment, etc.) must KEEP the exchange rate that was used at the time the transaction was created.
- When I enter a new daily rate the next day, existing transactions must NOT change/revalue using the new rate.
- Only cash/bank account balances (and account valuation) should change to reflect the latest daily rate (if the system is designed to revalue accounts daily).

Acceptance criteria (rate locking)
- Every transaction that depends on an exchange rate must store:
  - fx_rate_used (or equivalent)
  - base_currency_amount (if you use base currency)
  - foreign_currency_amount + currency_code
  - rate_date (or timestamp)
- Any screen/report that displays an old transaction must use its stored fx_rate_used and amounts, not the “current daily rate”.
- Updating daily rate must not mutate historical transaction totals, lines, receipts, or payment records.

2) Payments/receipts amount input is misinterpreting CFA as USD
- When selected currency is CFA and I create a payment or receipt, typing “50” is being treated as 50 USD instead of 50 CFA.
- Fix so amount inputs + payloads always respect the currently selected currency.

Acceptance criteria (CFA input parsing)
- If selected currency is CFA and user types “50”, the system must treat it as 50 CFA.
- UI label next to the input must show the selected currency.
- Validation/formatting/parsing must use selected currency (no hard-coded USD anywhere).
- API payloads must include the correct currency code and amount meaning.

Implementation steps (do these)
1) Scan repo for exchange rate + daily rate + fx logic and transaction creation:
   Search: “exchange”, “fx”, “rate”, “dailyRate”, “currency”, “USD”, “CFA”, “payment”, “receipt”, “pos”, “revalue”, “cash”, “bank”.
2) Identify where transactions currently fetch/use “current daily rate” at display time (this is the bug).
   - Refactor so the rate is captured at creation time and persisted on the transaction record.
3) Update DB/models/types for transaction records if missing fields:
   - currency_code, amount, base_amount, fx_rate_used, rate_date (names can differ)
4) Update transaction creation flows (POS/payment/receipt) to:
   - read selected currency
   - parse input as that currency
   - look up the current daily rate ONLY ONCE at creation time
   - store fx_rate_used and derived base_amount if applicable
5) Update transaction display + reports to use stored values (rate locked).
6) Update cash/bank account valuation logic to use latest daily rate (daily revaluation), without affecting historical transactions.
7) Add tests:
   - Create a payment at rate R1, then update daily rate to R2, confirm the payment totals remain based on R1.
   - Toggle to CFA, type “50”, confirm payload currency is CFA and amount is 50 CFA (not USD).

Output
- List all modified files and why.
- Explain how rates are stored and how revaluation works (transactions locked, accounts revalued).
- Provide a short manual QA checklist to verify POS/payment/receipt behavior.
