You are working inside my existing GitHub project. Please scan the entire codebase and add consistent time-period filters (Daily / Monthly / 1 Month / 6 Months / Yearly / Custom Range) to every page that shows time-based data (reports, ledgers, vouchers, POS daybook, inventory history, bank/cash accounts, dashboards, etc.).

Goal
Anywhere the UI shows lists/tables/charts that depend on dates, add a standard filter control so the user can quickly switch between:
- Today (Daily)
- This Month (Monthly)
- Last 1 Month
- Last 6 Months
- This Year (Yearly)
- Custom date range (From / To)

What you must do
1) Audit the repo for all pages that load data with dates:
   Search keywords: “date”, “fromDate”, “toDate”, “startDate”, “endDate”, “asOfDate”, “voucherDate”, “createdAt”, “updatedAt”, “daybook”, “report”, “ledger”, “statement”, “history”.
2) Identify every page that:
   - calls an API without a date range but should,
   - or has a date picker but no quick presets,
   - or shows large historical lists that need filtering.
3) Implement ONE reusable filter component:
   - `PeriodFilter` (or similar)
   - Presets: Today / This Month / Last 1 Month / Last 6 Months / This Year / Custom
   - Returns `{ fromDate, toDate, preset }`
   - Uses the same date format the backend expects (ISO `YYYY-MM-DD`)
4) Hook it into data fetching:
   - Update react-query `queryKey` to include `{ fromDate, toDate }`
   - Update `queryFn` to call endpoints with `?fromDate=...&toDate=...`
   - If endpoints don’t support it, update backend routes to accept these query params and filter in DB.
5) Make it consistent everywhere:
   - Same UI placement (top-right near Print/Export buttons or page header)
   - Same default (This Month) unless page-specific logic demands otherwise
   - Persist selection per page in URL query params (recommended) or localStorage

Backend requirements (if needed)
- For each relevant endpoint, add optional query params:
  - `fromDate` (inclusive) and `toDate` (inclusive)
- Filter using the correct date field:
  - vouchers: voucherDate
  - transactions: createdAt or transactionDate (whichever is correct in your schema)
  - reports: underlying voucher/entry dates
- Ensure filtering is timezone-safe and consistent.

Acceptance Criteria
- On every page with time-based data, user can select Daily/Monthly/1M/6M/Yearly/Custom and the table/report updates correctly.
- The filter applies to:
  - totals and summary numbers
  - list rows
  - exports/prints (export should export the filtered period)
- No page should require manual typing dates unless “Custom” is selected.

Deliverables
- List all pages you updated (file paths).
- List all API endpoints updated (file paths).
- Show the reusable PeriodFilter component code and 2–3 examples of integration.
- Provide a quick QA checklist (verify each preset on at least: vouchers/daybook, ledger/accounts, POS sales listing/report).
