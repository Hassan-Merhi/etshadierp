version: 2.1

orbs:
  node: circleci/node@7.1.0

executors:
  node22:
    docker:
      - image: cimg/node:22.14
    resource_class: medium+
    working_directory: ~/project
    environment:
      NODE_ENV: test
      SESSION_SECRET: circleci-session-secret
      CSRF_ENFORCE: "0"
      ENABLE_SCHEDULERS: "false"
      NODE_OPTIONS: --max-old-space-size=4096
      PORT: "5000"

  node22-postgres:
    docker:
      - image: cimg/node:22.14
        environment:
          NODE_ENV: test
          SESSION_SECRET: circleci-session-secret
          CSRF_ENFORCE: "0"
          ENABLE_SCHEDULERS: "false"
          NODE_OPTIONS: --max-old-space-size=2048
          PORT: "5000"
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/heliumdb
          PGSSLMODE: disable
      - image: cimg/postgres:15.13
        environment:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: heliumdb
    resource_class: medium+
    working_directory: ~/project

commands:
  install_dependencies:
    steps:
      - checkout
      - run:
          name: Normalize package registry URLs
          command: sed -i 's#http://package-firewall.replit.local/npm/#https://registry.npmjs.org/#g' package-lock.json
      - node/install-packages:
          pkg-manager: npm
          override-ci-command: npm ci --no-audit --bin-links=true
      - run:
          name: Dependency diagnostics
          command: |
            node --version
            npm --version
            npm config get bin-links
            node -p "'TypeScript ' + require('typescript/package.json').version"
            node -p "'jsdom ' + require('jsdom/package.json').version"
            for binary in tsc vite eslint vitest; do
              test -x "node_modules/.bin/$binary" || { echo "$binary is missing"; exit 1; }
            done

  wait_for_postgres:
    steps:
      - run:
          name: Wait for PostgreSQL
          command: |
            for attempt in $(seq 1 60); do
              if (echo > /dev/tcp/127.0.0.1/5432) >/dev/null 2>&1; then
                echo "PostgreSQL is accepting connections."
                exit 0
              fi
              sleep 1
            done
            echo "PostgreSQL did not become ready."
            exit 1

  prepare_postgres_schema:
    steps:
      - run:
          name: Verify migration registry
          command: node scripts/verify-migration-registry.mjs --json
      - run:
          name: Prepare disposable database schema
          command: node node_modules/drizzle-kit/bin.cjs push --force
      - run:
          name: Verify disposable database schema
          command: node scripts/verify-ci-disposable-schema.mjs

jobs:
  static-build:
    executor: node22
    steps:
      - install_dependencies
      - run:
          name: Verify lockfile and production dependencies
          command: |
            npm run verify:lockfile
            npm run verify:production-dependencies
      - run:
          name: Check formatting for changed source files
          command: |
            git fetch origin main --depth=1
            mapfile -d '' changed_files < <(
              git diff --name-only --diff-filter=ACMR -z origin/main...HEAD -- client/src server shared tests scripts |
                grep -zE '\.(ts|tsx|css|mjs)$' || true
            )
            if [ "${#changed_files[@]}" -eq 0 ]; then
              echo "No changed source files require formatting checks."
              exit 0
            fi
            printf 'Checking formatting for %s changed source file(s):\n' "${#changed_files[@]}"
            printf ' - %s\n' "${changed_files[@]}"
            node node_modules/prettier/bin/prettier.cjs --check "${changed_files[@]}"
      - run:
          name: Lint
          command: npm run lint
      - run:
          name: TypeScript
          command: npm run check
      - run:
          name: Production build
          command: npm run build
      - run:
          name: Verify server bundle
          command: npm run verify:server-bundle
      - run:
          name: Repository safety contracts
          command: |
            npm run verify:program1-observability
            npm run verify:final-production-readiness
            npm run verify:bandwidth
      - store_artifacts:
          path: dist
          destination: production-build

  phase4-format-file:
    parameters:
      target:
        type: string
    executor: node22
    steps:
      - install_dependencies
      - run:
          name: Check one Phase 4 file
          command: node node_modules/prettier/bin/prettier.cjs --check << parameters.target >>

  phase4-typescript:
    executor: node22
    steps:
      - install_dependencies
      - run:
          name: Phase 4 TypeScript diagnostic
          command: npm run check

  phase4-typescript-scope:
    parameters:
      targets:
        type: string
    executor: node22
    steps:
      - install_dependencies
      - run:
          name: Scoped Phase 4 TypeScript diagnostic
          command: node scripts/phase4-typecheck-scope.mjs << parameters.targets >>

  postgres-regression:
    executor: node22-postgres
    steps:
      - install_dependencies
      - wait_for_postgres
      - prepare_postgres_schema
      - run:
          name: Backend tests
          command: node node_modules/vitest/vitest.mjs run --maxWorkers=1 --no-file-parallelism
      - run:
          name: Frontend tests
          command: node node_modules/vitest/vitest.mjs run --config vitest.config.frontend.ts --maxWorkers=1 --no-file-parallelism

  phase4-backend:
    executor: node22-postgres
    steps:
      - install_dependencies
      - wait_for_postgres
      - prepare_postgres_schema
      - run:
          name: Phase 4 backend diagnostic
          command: >-
            node node_modules/vitest/vitest.mjs run
            tests/factory-arabic-translation-workbook.test.ts
            tests/factory-arabic-translation-routes.test.ts
            tests/operational-permission-route-policy.test.ts
            tests/route-manifest.test.ts
            --maxWorkers=1 --no-file-parallelism

  phase4-backend-case:
    parameters:
      target:
        type: string
      test_name:
        type: string
    executor: node22-postgres
    steps:
      - install_dependencies
      - wait_for_postgres
      - prepare_postgres_schema
      - run:
          name: Run one Phase 4 backend case
          command: >-
            node node_modules/vitest/vitest.mjs run
            << parameters.target >>
            -t "<< parameters.test_name >>"
            --maxWorkers=1 --no-file-parallelism

  phase4-frontend:
    executor: node22
    steps:
      - install_dependencies
      - run:
          name: Phase 4 frontend diagnostic
          command: >-
            node node_modules/vitest/vitest.mjs run
            --config vitest.config.frontend.ts
            tests/ui/factory-arabic-translation-actions.test.tsx
            --maxWorkers=1 --no-file-parallelism

  security-readiness:
    executor: node22-postgres
    steps:
      - install_dependencies
      - wait_for_postgres
      - run:
          name: Prepare disposable database schema
          command: node node_modules/drizzle-kit/bin.cjs push --force
      - run:
          name: Critical dependency audit
          command: npm audit --audit-level=critical
      - run:
          name: Production dependency verification
          command: npm run verify:production-dependencies
      - run:
          name: Program 6 security checks
          command: PROGRAM6_SKIP_TYPESCRIPT=1 npm run check:program-6-security
      - run:
          name: Final production readiness contract
          command: npm run verify:final-production-readiness

workflows:
  full-erp-verification:
    jobs:
      - static-build
      - phase4-format-file:
          name: p4-fmt-actions
          target: client/src/components/FactoryArabicTranslationActions.tsx
      - phase4-format-file:
          name: p4-fmt-hub
          target: client/src/pages/factory/FactoryBalesHub.tsx
      - phase4-format-file:
          name: p4-fmt-routes
          target: server/routes/factory/factoryArabicTranslationRoutes.ts
      - phase4-format-file:
          name: p4-fmt-catalog
          target: server/routes/factory/factoryBilingualCatalogRoutes.ts
      - phase4-format-file:
          name: p4-fmt-workbook
          target: server/services/factoryArabicTranslationWorkbook.ts
      - phase4-format-file:
          name: p4-fmt-policy
          target: server/services/security/operationalPermissionRoutePolicy.ts
      - phase4-format-file:
          name: p4-fmt-route-tests
          target: tests/factory-arabic-translation-routes.test.ts
      - phase4-format-file:
          name: p4-fmt-workbook-tests
          target: tests/factory-arabic-translation-workbook.test.ts
      - phase4-format-file:
          name: p4-fmt-policy-tests
          target: tests/operational-permission-route-policy.test.ts
      - phase4-format-file:
          name: p4-fmt-ui-tests
          target: tests/ui/factory-arabic-translation-actions.test.tsx
      - phase4-format-file:
          name: p4-fmt-script
          target: scripts/phase4-typecheck-scope.mjs
      - phase4-typescript
      - phase4-typescript-scope:
          name: p4-ts-client
          targets: >-
            client/src/components/FactoryArabicTranslationActions.tsx
            client/src/pages/factory/FactoryBalesHub.tsx
      - phase4-typescript-scope:
          name: p4-ts-routes
          targets: >-
            server/routes/factory/factoryArabicTranslationRoutes.ts
            server/routes/factory/factoryBilingualCatalogRoutes.ts
      - phase4-typescript-scope:
          name: p4-ts-workbook
          targets: server/services/factoryArabicTranslationWorkbook.ts
      - phase4-typescript-scope:
          name: p4-ts-policy
          targets: server/services/security/operationalPermissionRoutePolicy.ts
      - postgres-regression
      - phase4-backend
      - phase4-backend-case:
          name: p4-wb-export
          target: tests/factory-arabic-translation-workbook.test.ts
          test_name: exports the exact template columns
      - phase4-backend-case:
          name: p4-wb-roundtrip
          target: tests/factory-arabic-translation-workbook.test.ts
          test_name: round-trips article codes
      - phase4-backend-case:
          name: p4-wb-headers
          target: tests/factory-arabic-translation-workbook.test.ts
          test_name: rejects workbooks whose columns
      - phase4-backend-case:
          name: p4-wb-exact
          target: tests/factory-arabic-translation-workbook.test.ts
          test_name: matches by normalized exact article code only
      - phase4-backend-case:
          name: p4-wb-duplicates
          target: tests/factory-arabic-translation-workbook.test.ts
          test_name: blocks duplicate article codes
      - phase4-backend-case:
          name: p4-wb-ambiguous
          target: tests/factory-arabic-translation-workbook.test.ts
          test_name: blocks ambiguous normalized article codes
      - phase4-backend-case:
          name: p4-wb-category
          target: tests/factory-arabic-translation-workbook.test.ts
          test_name: blocks conflicting translations
      - phase4-backend-case:
          name: p4-wb-idempotent
          target: tests/factory-arabic-translation-workbook.test.ts
          test_name: is idempotent when translations already match
      - phase4-backend-case:
          name: p4-wb-fill
          target: tests/factory-arabic-translation-workbook.test.ts
          test_name: does not replace existing Arabic values
      - phase4-backend-case:
          name: p4-wb-unknown
          target: tests/factory-arabic-translation-workbook.test.ts
          test_name: reports unknown article codes
      - phase4-backend-case:
          name: p4-wb-empty
          target: tests/factory-arabic-translation-workbook.test.ts
          test_name: rejects empty translation rows
      - phase4-backend-case:
          name: p4-wb-token
          target: tests/factory-arabic-translation-workbook.test.ts
          test_name: changes the preview token
      - phase4-backend-case:
          name: p4-wb-errors
          target: tests/factory-arabic-translation-workbook.test.ts
          test_name: creates an error workbook
      - phase4-backend-case:
          name: p4-route-auth
          target: tests/factory-arabic-translation-routes.test.ts
          test_name: keeps authentication in front
      - phase4-backend-case:
          name: p4-route-export
          target: tests/factory-arabic-translation-routes.test.ts
          test_name: exports a protected company-scoped
      - phase4-backend-case:
          name: p4-route-token
          target: tests/factory-arabic-translation-routes.test.ts
          test_name: requires a preview token
      - phase4-backend-case:
          name: p4-route-apply
          target: tests/factory-arabic-translation-routes.test.ts
          test_name: applies only Arabic fields
      - phase4-backend-case:
          name: p4-route-stale
          target: tests/factory-arabic-translation-routes.test.ts
          test_name: rejects a stale preview
      - phase4-backend-case:
          name: p4-route-duplicate
          target: tests/factory-arabic-translation-routes.test.ts
          test_name: blocks duplicate workbook article codes
      - phase4-backend-case:
          name: p4-route-rollback
          target: tests/factory-arabic-translation-routes.test.ts
          test_name: rolls product and category updates back
      - phase4-backend-case:
          name: p4-route-xlsx
          target: tests/factory-arabic-translation-routes.test.ts
          test_name: rejects non-xlsx uploads
      - phase4-frontend
      - security-readiness:
          requires:
            - static-build
